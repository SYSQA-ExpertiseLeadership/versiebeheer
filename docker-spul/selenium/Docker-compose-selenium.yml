version: "3"
services:
  selenium:
    build:
      context: /var/opt/test/selenium/
      dockerfile: Dockerfile-selenium
    image: selenium-test-cicd-training
    networks: 
      - website_proxy
      
  networks:
    website_proxy:
      external: true